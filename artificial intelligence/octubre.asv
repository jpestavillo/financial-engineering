%gradiente descendiente  w(t+1)=w(t)-(NderivadaJ/derivadaW)
%J=1/n [sumatoria(i:n) -Yi LN(Yi^)-(1-Yi)ln(1-Yi^)]
%derivada J/derivada W=0
%derivada J/derivadaW=derivada J/derivada y^  derivada y^/derivadav derivada v/derivada w

exactitud 
precision 
recall
